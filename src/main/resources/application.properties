# Database - PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/ecommerce_db
spring.datasource.username=postgres
spring.datasource.password=Aparna@123
spring.datasource.driver-class-name=org.postgresql.Driver

# Elasticsearch
spring.elasticsearch.uris=https://localhost:9200
spring.elasticsearch.username=elastic
spring.elasticsearch.password=JIJLg_ay8sWWsXFZmq7*
spring.elasticsearch.connection-timeout=5s
spring.elasticsearch.socket-timeout=30s

# Trust local self-signed Elasticsearch certificate
spring.elasticsearch.restclient.ssl.bundle=elasticsearch
spring.ssl.bundle.pem.elasticsearch.truststore.certificate=file:C:/Users/choud/Downloads/elasticsearch-9.3.3-windows-x86_64/elasticsearch-9.3.3/config/certs/http_ca.crt

# JPA
sendgrid.api.key=YOUR_REAL_API_KEY
spring.jpa.show-sql=true
spring.cache.type=none
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

spring.jpa.hibernate.ddl-auto=update

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=false
spring.flyway.locations=classpath:db/migration

# JWT
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.access-token-expiry=900000
jwt.refresh-token-expiry=1209600000

razorpay.key.id=${RAZORPAY_ID}
razorpay.key.secret=${RAZORPAY_SECRET}


twilio.account.sid=${TWILIO_SID}
twilio.auth.token=${TWILIO_AUTH_TOKEN}
twilio.phone.number=+17434002258
# Cookie
app.cookie.secure=false
app.cookie.same-site=Strict

# App base URL
app.base-url=http://localhost:8080

# Redis



# Cart TTL
app.cart.guest-ttl-days=7
app.cart.user-ttl-days=30